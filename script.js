// Add event listeners for pixel clicks
const pixels = document.querySelectorAll('.pixel');
pixels.forEach(pixel => {
    pixel.addEventListener('click', () => {
        // Add your code to handle pixel click events here
        // For example, change the pixel color or perform other actions
    });
});
